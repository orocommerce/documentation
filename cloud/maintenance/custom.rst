Custom Commands
===============

To run an application command not covered by the maintenance tool, run it via the `app:console` command, for example:

.. code-block:: none
    :linenos:

    orocloud-cli app:console oro:user:list
